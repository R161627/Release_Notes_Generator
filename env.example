# Date Range for Release Notes Generation
# Format: YYYY-MM-DD
START_DATE=2024-01-01
END_DATE=2024-01-31

# GitLab Configuration
# Your GitLab instance URL (without trailing slash)
GITLAB_URL=https://your-gitlab-instance.com

# GitLab Project ID (can be found in project settings)
GITLAB_PROJECT_ID=your-project-id

# GitLab Personal Access Token
# Create at: GitLab → User Settings → Access Tokens
# Required scopes: read_api, read_repository
GITLAB_TOKEN=your-gitlab-access-token

# JIRA Configuration
# Your JIRA instance URL (without trailing slash)
JIRA_SERVER=https://your-jira-instance.com

# JIRA API Token
# Create at: JIRA → Account Settings → Security → API Tokens
JIRA_TOKEN=your-jira-api-token

